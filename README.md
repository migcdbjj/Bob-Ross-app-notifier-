# Bob Ross App Watcher

Monitors which app you're focused on. After 1 hour on the same app, 
a floating window appears with a random Bob Ross quote and painting 
fetched from a dataset of all 403 Joy of Painting episodes.

## Requirements
sudo apt install python3-tk
pip install -r requirements.txt

## Usage
python3 bob_ross_watcher.py
